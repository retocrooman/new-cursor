# TODO

- モノレポ土台
  - [x] monorepo-template を本リポジトリへ移行できる
  - [x] Turborepo + pnpm で `@new-cursor/*` スコープの apps/packages を起動できる
  - [x] テンプレートの item/user サンプルドメインを除外できる
- ローカル起動
  - [x] docker-compose で DB と ElasticMQ を起動できる
  - [x] apps/web・apps/worker・apps/relay をホストで `pnpm dev` 起動できる
  - [x] ヘルスチェックで各サービスの状態を確認できる
- 基本データ
  - [x] リポジトリを登録できる（外部 repo は worktree_requested で lazy clone）
  - [x] エージェントを作成し、複数ラベルを付与できる
  - [x] エージェントごとに購読するイベント種別を設定できる
  - [ ] エージェントごとに購読フィルタ（payload 条件）を設定できる
  - [x] ルールをラベル付きでDBに保存できる
  - [x] タスクと実行記録をDBに保存できる（run.started まで）
  - [x] MVP 認証（better-auth）でログインできる
- アウトボックス・relay・インボックス
  - [x] 司令室がトランザクション内で outbox にイベントを書ける
  - [x] apps/relay が outbox を ElasticMQ に publish できる
  - [x] Worker が SQS から受け取り inbox で冪等処理できる
- イベント処理
  - [x] SQSイベントでWorkerを起動できる
  - [ ] 承認イベントで待機中タスクを再開できる
  - [ ] PRマージイベントでタスクを完了できる
  - [ ] CI結果とコンフリクトをタスク状態に反映できる
  - [ ] cronで定期タスクを起動できる
  - [x] 購読条件に一致するエージェントへイベントをルーティングできる
  - [x] 固定工程とイベントフローを併用してタスクを進行できる（partial — stage transition only）
  - [ ] イベントフロー定義をDBスキーマとoRPC CRUDで管理できる（UIは後回し）
- タスク起票
  - [ ] 司令官チャットからタスクを起票できる
  - [ ] apps/web 内の専用 Cursor Agent SDK で司令官チャットを実行できる
  - [ ] 起票時にタイトル・ベースブランチ・ブランチ名・背景・検証項目を確認できる
- タスク実行
  - [x] タスクごとに WORKTREE_ROOT 上に git worktree とブランチを作成できる
  - [x] 同一リポジトリ・同一ブランチの競合タスクを直列キューで処理できる（queued 解放まで）
  - [ ] エスカレーション：親ブロック→子タスク→親再開ができる
  - [x] DBから共通ルールとエージェント別ルールを取得し、プロンプトに注入できる
  - [x] ホスト上の Local Cursor SDK で worktree を cwd としてタスクを実行できる
  - [ ] 変更をコミットし、PRを作成できる
  - [ ] UI 承認ボタンと PR auto-merge で完了できる
- UI
  - [ ] 3 カラム（タスクリスト | タスク詳細 | 司令官）を表示できる
  - [ ] タスクリストに親タスクのみ・2 行表示（タイトル+stage、repo·最後のイベント）を表示できる
  - [ ] タスク詳細に工程パイプライン・背景・検証・意思決定・統合タイムライン・予測イベントフローを表示できる
  - [ ] 司令官に Cursor 風 UI・送信・モデルピッカー・選択タスク文脈を備える
  - [ ] 意思決定を `task_decisions` 経由で表示・追記できる（Commander / Worker の record_decision）
- 外部連携
  - [ ] GitHubのPR作成・CI・マージイベントを扱える
  - [ ] Slackから通知・承認・起票ができる
  - [ ] Confluenceから仕様や背景を参照できる
  - [ ] MCPプロファイルを工程ごとに切り替えられる
- 自己改善
  - [ ] タスク終了後に振り返りを作成できる
  - [ ] 実行記録からルール候補を保存できる
  - [ ] cronでルールと注入方法を見直せる
