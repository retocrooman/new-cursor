# TODO

- モノレポ土台
  - [x] monorepo-template を本リポジトリへ移行できる
  - [x] Turborepo + pnpm で `@new-cursor/*` スコープの apps/packages を起動できる
  - [x] テンプレートの item/user サンプルドメインを除外できる
- ローカル起動
  - [x] docker-compose で DB と ElasticMQ を起動できる
  - [x] apps/web・apps/worker・apps/relay をホストで `pnpm dev` 起動できる
  - [x] ヘルスチェックで各サービスの状態を確認できる
- 基本データ
  - [ ] リポジトリを登録し、外部 repo は clone できる
  - [ ] エージェントを作成し、複数ラベルを付与できる
  - [ ] エージェントごとに購読するイベント種別やフィルタを設定できる
  - [ ] ルールをラベル付きでDBに保存できる
  - [ ] タスクと実行記録をDBに保存できる（ES/CQRS は task + run のみ）
  - [ ] MVP 認証（better-auth）でログインできる
- アウトボックス・relay・インボックス
  - [ ] 司令室がトランザクション内で outbox にイベントを書ける
  - [ ] apps/relay が outbox を ElasticMQ に publish できる
  - [ ] Worker が SQS から受け取り inbox で冪等処理できる
- イベント処理
  - [ ] SQSイベントでWorkerを起動できる
  - [ ] 承認イベントで待機中タスクを再開できる
  - [ ] PRマージイベントでタスクを完了できる
  - [ ] CI結果とコンフリクトをタスク状態に反映できる
  - [ ] cronで定期タスクを起動できる
  - [ ] 購読条件に一致するエージェントへイベントをルーティングできる
  - [ ] 固定工程とイベントフローを併用してタスクを進行できる
  - [ ] イベントフロー定義をDBスキーマとoRPC CRUDで管理できる（UIは後回し）
- タスク起票
  - [ ] 司令官チャットからタスクを起票できる
  - [ ] apps/web 内の専用 Cursor Agent SDK で司令官チャットを実行できる
  - [ ] 起票時にタイトル・ベースブランチ・ブランチ名・背景・検証項目を確認できる
- タスク実行
  - [ ] タスクごとに WORKTREE_ROOT 上に git worktree とブランチを作成できる
  - [ ] 同一リポジトリ・同一ブランチの競合タスクを直列キューで処理できる
  - [ ] エスカレーション：親ブロック→子タスク→親再開ができる
  - [ ] DBから共通ルールとエージェント別ルールを取得し、プロンプトに注入できる
  - [ ] ホスト上の Local Cursor SDK で worktree を cwd としてタスクを実行できる
  - [ ] 変更をコミットし、PRを作成できる
  - [ ] UI 承認ボタンと PR auto-merge で完了できる
- UI
  - [ ] 左にタスクリスト、中央に詳細・ダッシュボード、右に司令官チャットを表示できる
  - [ ] タスクリストにレポジトリ名・状態・実行時間・消費トークン数・最後のイベントを表示できる
  - [ ] ダッシュボードに意思決定・イベント履歴・作業ファイル・変更量・ブランチ名を表示できる
- 外部連携
  - [ ] GitHubのPR作成・CI・マージイベントを扱える
  - [ ] Slackから通知・承認・起票ができる
  - [ ] Confluenceから仕様や背景を参照できる
  - [ ] MCPプロファイルを工程ごとに切り替えられる
- 自己改善
  - [ ] タスク終了後に振り返りを作成できる
  - [ ] 実行記録からルール候補を保存できる
  - [ ] cronでルールと注入方法を見直せる
